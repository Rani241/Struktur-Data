a. Q ke simpul (1)
Langsung arahkan ke simpul pertama:
Q = FIRST
b. Q ke simpul (7)
Mulai dari simpul pertama, geser enam kali ke depan:
Q = FIRST
ulang 6 kali:
    Q = Q->LINK
c. Q ke simpul terakhir
Langsung salin dari pointer LAST:
Q = LAST
d. Q ke simpul dengan INFO = 50
Telusuri dari awal sampai menemukan simpul dengan nilai 50:

Q = FIRST
selama (Q->INFO != 50) dan (Q != NULL):
    Q = Q->LINK


e. Q ke simpul di depan simpul dengan INFO = 50
Telusuri sampai simpul berikutnya punya nilai 50:

Q = FIRST
selama (Q->LINK->INFO != 50) dan (Q->LINK != NULL):
    Q = Q->LINK


Selesai — sekarang Q berada di posisi yang diminta sesuai tiap kondisi.